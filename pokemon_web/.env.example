# 이 파일을 복사해 .env 로 저장한 뒤 값을 채우세요.
#   복사: copy .env.example .env   (Windows)
# .env 는 git에 커밋되지 않습니다(.gitignore 등록됨).

# 관리자 전용 DB 재빌드 API(/admin/rebuild-db) 토큰.
# 값을 비워두거나 설정하지 않으면 해당 API는 비활성화되어 404를 반환합니다.
ADMIN_TOKEN=change-this-to-your-secret
